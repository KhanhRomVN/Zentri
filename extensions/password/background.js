/**
 * ------------------------------------------------------------------
 * Zentri Password Autofill — service worker (skeleton)
 * ------------------------------------------------------------------
 * Reserved for future cross-frame coordination, storage reads, and
 * message routing between content scripts and the extension's data
 * file. Currently only logs on startup so we can confirm the MV3
 * service worker registers without errors.
 * ------------------------------------------------------------------
 */

console.log('[Zentri Password Autofill] service worker started');